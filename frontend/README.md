Frontend (React) outline

Purpose
- Search routes
- Show price trend
- Track a route
- Show loading + error states

Planned layout
- src/
  - pages/ (search, track, history)
  - components/ (form, cards, chart)
  - hooks/ (data fetching)
  - services/ (API client)
  - state/ (store or context)
  - styles/
- public/
- tests/
