# FlightWatch Backend Dependencies
fastapi==0.104.1
uvicorn==0.24.0
python-dotenv==1.0.0
amadeus==7.0.0

# GCP Cloud Storage support (optional - for team's GCS data)
google-cloud-storage==2.16.0
# Note: pandas removed due to Python 3.13 compatibility issues
# Use csv module instead for simple CSV parsing

flask==3.0.3
gunicorn==22.0.0
google-cloud-firestore==2.16.0
sendgrid==6.11.0

# Data collection (Google Flights scraper, no API key needed)
fast-flights==2.2

# Rate limiting
slowapi==0.1.9

# Testing
pytest==8.3.4
httpx==0.25.2